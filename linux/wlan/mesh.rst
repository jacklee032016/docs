主要已经完成的工作
=====================
* MESH Portal接口（Portal接口与桥接口的控制）;
* Portal接口数据的收发（Wifi帧到ethernet帧的转换）;
* 转发控制函数的注册功能;
* 转发操作中的tasklet;
* Ad Hoc和AP的associated的站对转发表的注册;
* 桥表到MESH转发表的同步;
* MESH层基本的查表和转发操作;

MESH实现的基本结构
======================
* 完全按802.11s方式实现;
* 在802.11之上单独的mesh层;
* 单纯的二层转发;

MAP的实现
==============
* MESH是基于Ad Hoc模式;
* AP模式的实现;

帧的封装
==============
* 按802.11s的MESH帧
   * （Action帧、MESH 数据帧、beacon帧的MESH字段）
   * AP模式下如何兼容MAP模式
* 单纯作为数据帧的形式
   * 结合ethernet对IP的封装和WIFI的Ad Hoc的帧格式


拥塞控制

QoS管理


与桥更加紧密地结合



下一阶段主要任务
====================
* 纯粹的二层转发;
* MAP的实现;
* Portal树的管理;
* 帧格式的定义和处理;
* Ad Hoc的实现;

